import { createSlice } from '@reduxjs/toolkit'

const initialState = {
}

const courses = createSlice({
  name: second,
  initialState,
  reducers: {

  }
});

export const {} = courses.actions

export default courses.reducer